/**
 * Script de notarização para macOS (opcional)
 * Por enquanto apenas um placeholder
 */

async function notarize(context) {
    // Placeholder para futuras implementações de code signing
    console.log('ℹ️ Notarization skipped (not implemented yet)');
    return;
}

module.exports = notarize;